#ifndef  VIDEOCAPTURE_H
#define  VIDEOCAPTURE_H

#include "util.h"

namespace SonixLib{


extern "C" {
///////////////////////////////////////////////////////////////////////////////////////////////////////////////
//库初始化
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_Init
*/
	bool WinAPI SonixCam_Init();


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//库反初始化
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_UnInit
*/
	bool WinAPI SonixCam_UnInit();


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得上一次操作失败后返回的错误代码
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetErrorCode	
*	Parameters:		
*							dev: 要操作的设备指针
*							ec: 函数调用失败时返回的错误代码。
*/
	bool WinAPI SonixCam_GetErrorCode(scDevice* dev, scErrorCode* ec);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//枚举所有SONIX摄像头设备
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_EnumCameras
*	Parameters:		
*							devCount: 枚举sonix设备，返回枚举到的设备数量
*							devs: 所有枚举到的sonix设备信息会填充到devs里面。
*							devsArraySize: ARRAYSIZE(devs)数组元素个数，也就是最大枚举设备数量。
*/
	bool WinAPI SonixCam_EnumCameras(uint* devCount, scDevice devs[], uint devsArraySize);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设备重启
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_Restart	
*	Parameters:		
*							dev: 要操作的设备指针
*/
	bool WinAPI SonixCam_Restart(scDevice* dev);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//读寄存器
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_AsicRegisterRead
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 读寄存器开始地址
*							data: 读到的数据缓存指针
*							dataLen: 读数据字节数
*/
	bool WinAPI SonixCam_AsicRegisterRead(scDevice* dev, ushort addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//写寄存器
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_AsicRegisterWrite	
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 写寄存器开始地址
*							data: 保存要写到寄存器的数据缓存指针
*							dataLen: 写数据字节数
*/
	bool WinAPI SonixCam_AsicRegisterWrite(scDevice* dev, ushort addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//I2C读
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ReadFromI2C	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							data: 数据缓存数组指针
*							dataLen: 读数据字节数
*/
	bool WinAPI SonixCam_ReadFromI2C(scDevice* dev, uchar slaveID, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//I2C写
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_WriteToI2C	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							data: 数据缓存数组指针
*							dataLen: 写数据字节数
*/
	bool WinAPI SonixCam_WriteToI2C(scDevice* dev, uchar slaveID, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//带地址的I2C读
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CstmReadFromI2C	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							addr: 读的地址
*							addrBytes: 地址的长度
*							data: 数据缓存数组指针
*							dataLen: 读数据字节数
*/
	bool WinAPI SonixCam_CstmReadFromI2C(scDevice* dev, uchar slaveID, uint addr, uchar addrBytes, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//带地址的I2C写
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CstmWriteToI2C	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
* 						addr: 写的地址
*							addrBytes: 地址的长度
*							data: 数据缓存数组指针
*							dataLen: 写数据字节数
*/
	bool WinAPI SonixCam_CstmWriteToI2C(scDevice* dev, uchar slaveID, uint addr, uchar addrBytes, uchar data[], uint dataLen);



////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//I2C读，比SonixCam_ReadFromI2C快, 地址是1byte(286,288,289,299支持多byte地址)，addrBytes + dataLen <= 5bytes(286,288,289,299 addrBytes + dataLen <= 8bytes)
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SensorRead	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							addr: 虚写的地址，一般是地址或者是格式命令
*							addrBytes: 虚写的地址字节数
*							data: 读数据缓存数组指针
*							dataLen: 读数据字节数
*/
	bool WinAPI SonixCam_SensorRead(scDevice* dev, uchar slaveID, uint addr, uchar addrBytes, uchar data[], uchar dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//I2C写, 比SonixCam_WriteToI2C快, 地址是1byte(286,288,289,299支持多byte地址)，addrBytes + dataLen <= 5bytes(286,288,289,299 addrBytes + dataLen <= 8bytes)
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SensorWrite	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							addr: 写的地址，一般是地址或者是格式命令
*							addrBytes: 虚写的数据字节数
*							data: 写数据缓存数组指针
*							dataLen: 写数据字节数
*/
	bool WinAPI SonixCam_SensorWrite(scDevice* dev, uchar slaveID, uint addr, uchar addrBytes, uchar data[], uchar dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//第二组I2C读
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ReadFromI2C2	
*	Parameters:		
*							dev: 要操作的设备指针
*							slaveID: slave设备地址
*							data: 数据缓存数组指针
*							dataLen: 读数据字节数
*/
	bool WinAPI SonixCam_ReadFromI2C2(scDevice* dev, uchar slaveID, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//第二组I2C写
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_WriteToI2C2	
*	Parameters:		dev: 要操作的设备指针
*					slaveID: slave设备地址
*					data: 要写的数据缓存数组指针
*					dataLen: 写数据字节数
*//////////////////////////////////
//XU 获得节点ID
////////////////////////////////////////////////////////////////////////////////
	bool WinAPI SonixCam_WriteToI2C2(scDevice* dev, uchar slaveID, uchar data[], uint dataLen);


///////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetNodeID
*	Parameters:		
*							dev: 要操作的设备指针
* 						nodeID: 返回获得到的节点ID
*							unitID: USB Control Transfer Unit ID
*/
	bool WinAPI SonixCam_GetNodeID(scDevice* dev, uchar* nodeID, uchar unitID = 0x03); //unitID = 0x04 


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//XU 读, 用于自定义扩展XU的读数据
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_XuRead
*	Parameters:		
*							dev: 要操作的设备指针
*							data: 读到的数据保存到data所指向的缓存中
*							dataLen: 读数据字节数
*							cs: USB Control Select
* 						nodeID: SonixCam_GetNodeID获得到的Node ID
*							unitID: USB Control Transfer Unit ID
*/
	bool WinAPI SonixCam_XuRead(scDevice* dev, uchar data[], uint dataLen, uchar cs, uchar nodeID, uchar unitID = 0x03); //unitID = 0x04 


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//XU 写, 用于自定义扩展XU的写数据
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_XuWrite
*	Parameters:		
*							dev: 要操作的设备指针
*							data:  数据缓存指针
*							dataLen: 写数据字节数
*							cs: USB Control Select
*							nodeID: SonixCam_GetNodeID可以获得到nodeID
*							unitID: USB Control Transfer Unit ID
*/
	bool WinAPI SonixCam_XuWrite(scDevice* dev, uchar data[], uint dataLen, uchar cs, uchar nodeID, uchar unitID = 0x03);  //unitID = 0x04 


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Serial Flash 读
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ReadFromSF	
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 读开始地址
*							data: 读取的数据保存到data指针指向的缓存里面
*							dataLen: 要读取的数据长度
*/
	bool WinAPI SonixCam_ReadFromSF(scDevice* dev, uint addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Serial Flash 写
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_WriteToSF	
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 写开始地址
*							data: 保存了将要写入到Flash里面的数据指针
*							dataLen: 要写入的数据长度
*/
	bool WinAPI SonixCam_WriteToSF(scDevice* dev, uint addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Serial Flash 读, 速度比SonixCam_ReadFromSF慢
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CstmReadFromSF	
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 写开始地址
*							data: 保存了将要写入到Flash里面的数据指针
*							dataLen: 要写入的数据长度
*/
	bool WinAPI SonixCam_CstmReadFromSF(scDevice* dev, uint addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Serial Flash 写， 速度比SonixCam_WriteToSF慢
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CstmWriteToSF	
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 写开始地址
*							data: 保存了将要写入到Flash里面的数据指针
*							dataLen: 要写入的数据长度
*/
	bool WinAPI SonixCam_CstmWriteToSF(scDevice* dev, uint addr, uchar data[], uint dataLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得 Serial Flash 的 RDID
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetSerialFlashRDID	
*	Parameters:		
*							dev: 要操作的设备指针
*							mf: 写开始地址
*							devID1: 设备ID1
*							devID2: 设备ID2
*							dummyNum: 
*							devIDNum: 设备ID数量
*							cmd: Serial Flash读RDID命令
*/
	bool WinAPI SonixCam_GetSerialFlashRDID(scDevice* dev, uchar* mf, uchar* devid1, uchar* devid2, uchar dummyNum, uchar devidNum, uchar cmd = SFCMD_RDID_MXIC_ALINK);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得固件版本号
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetVersion	
*	Parameters:		
*							dev: 要操作的设备指针
*							verBuf: 存储读到的固件版本字符串缓存指针
*							verBufLen: verBuf的缓存大小,ARRAYSIZE(verBuf)
*							fwBuf: 为0是从设备读取固件版本号，不为0时是从fwBuf指向的固件缓存里面读取固件版本号。
*/
	bool WinAPI SonixCam_GetVersion(scDevice* dev, uchar verBuf[], uint verBufLen, uchar* fwBuf = 0);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得设备描述字符串
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetDevDescriptor
*	Parameters:		
*							dev: 要操作的设备指针
*							parmType: 要获得的设备描述字符串参数类型
*							data: 保存读到的字符串缓存指针
*							dataBufLen: ARRAYSIZE(data)
*							fwBuf: 为0时从设备里面读取，不为0时从fwBuf指向的固件缓存里面读取。
*							fwLen: fwBuf固件缓冲区大小
*/
	bool WinAPI SonixCam_GetDevDescriptor(scDevice* dev, scParamType parmType, uchar data[], uint dataBufLen, uchar* fwBuf = 0, uint fwLen = 0);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	检查设备是否在开流状态
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CheckStreamOn
*	Parameters:		
*							dev: 要操作的设备指针
*							rVal: 返回当前是否是开流状态，如果rVal == 1，开流中。
*/
	bool WinAPI SonixCam_CheckStreamOn(scDevice* dev, bool* isOpening);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置Serial flash 寄存器相关命令
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetStatusRegBitWP
*	Parameters:		
*							dev: 要操作的设备指针
*							gpioWP: Flash硬件写保护位(WP GPIO位, 可以设置为Ox01，0x02，0x04，0x08，0x10，0x20，0x40，0x80)
*							rsrCMD: 读状态寄存器命令
*							wsrCMD: 写状态寄存器命令
*							sfWriteEnableCMD: 写Enable命令
*							sfPageProgramCMD: 页编程命令
*							sfReadDataCMD: 读数据命令
*/
	bool WinAPI SonixCam_SetStatusRegBitWP(scDevice* dev, uchar gpioWP = 0x02, uchar rsrCMD = 0x05, uchar wsrCMD = 0x01, uchar sfWriteEnableCMD = 0x06, uchar sfPageProgramCMD = 0x02, uchar sfReadDataCMD = 0x03);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置Serial flash开关写保护
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetWriteProtectedSF
*	Parameters:		
*							dev: 要操作的设备指针
*							disable: 设置为true，关闭SerialFlash写保护，设为false，开启SerialFlash写保护
*/
	bool WinAPI SonixCam_SetWriteProtectedSF(scDevice* dev, bool disableWP = true);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	设置Flash扇区大小和扇区擦除命令, Serial Flash默认扇区大小一般是4k
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetSectorInfoSF
*	Parameters:		
*							dev: 要操作的设备指针
*							sfSectorSize: Serial Flash扇区大小
*							sfSectorEraseCMD: Serial Flash扇区擦除命令
*/
	bool WinAPI SonixCam_SetSectorInfoSF(scDevice* dev, uint sfSectorSize, uchar sfSectorEraseCMD = 0x20);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	设置Flash块区大小和块区擦除命令, Serial Flash默认块区大小一般是64k
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetBlockInfoSF
*	Parameters:		
*							dev: 要操作的设备指针
*							sfBlockSize: Serial Flash块区大小
*							sfBlockEraseCMD: Serial Flash块区擦除命令
*/
	bool WinAPI SonixCam_SetBlockInfoSF(scDevice* dev, uint sfBlockSize, uchar sfBlockEraseCMD = 0xd8);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Serial Flash 扇区擦除
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SectorEraseSF
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: Serial Flash扇区擦除
*							eraseCMD: 扇区擦除命令
*/
	bool WinAPI SonixCam_SectorEraseSF(scDevice* dev, uint addr, uchar eraseCMD = 0x20);  //eraseCode = 0xd7, eraseCode = 0xd8


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Serial Flash 块区擦除
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_BlockEraseSF
*	Parameters:		
*							dev: 要操作的设备指针
* 						addr: 要擦除的块区地址
*							eraseCMD: 块区擦除命令
*/
	bool WinAPI SonixCam_BlockEraseSF(scDevice* dev, uint addr, uchar eraseCMD = 0xd8);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Serial Flash 块区擦除
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_HalfBlockEraseSF
*	Parameters:		
*							dev: 要操作的设备指针
*							addr: 要擦除的块区地址
*							eraseCMD: 块区擦除命令
*/
	bool WinAPI SonixCam_HalfBlockEraseSF(scDevice* dev, uint addr, uchar eraseCMD = 0x52);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	Serial Flash 芯片擦除
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ChipEraseSF
*	Parameters:		
*							dev: 要操作的设备指针
*							eraseCMD: 芯片擦除命令
*/
	bool WinAPI SonixCam_ChipEraseSF(scDevice* dev, uchar eraseCMD = 0x60);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	烧录固件,不要在开图的情况下调用该函数。
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_BurnerFW	
*	Parameters:		
*							dev: 要操作的设备指针
*							fwBuf: 固件缓存指针
*							fwLen: 固件缓存长度
*							setProgress: 烧录进度回调函数指针
*							ptrClass: 传递给setProgress的参数指针
*							fullCheckFW: 烧录完成后是否进行全校验
*/
	bool WinAPI SonixCam_BurnerFW(scDevice* dev, uchar fwBuf[], uint fwLen, scDevDescriptor* parmInfo, SonixCam_SetProgress setProgress, void* ptrClass, bool fullCheckFW = false);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	修改固件文件的设备描述字符串
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetDevDescriptorToBinFile
*	Parameters:
*							parmInfo: 烧录进度回调函数指针
*							fwBuf: 固件缓存长度
*							fwLen: 烧录进度回调函数指针
*/
	bool WINAPI SonixCam_SetDevDescriptorToBinFile(scDevDescriptor* parmInfo, uchar* fwBuf, uint fwLen);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	修改设备固件的设备描述字符串
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetDevDescriptor
*	Parameters:		
*							dev: 操作的设备参数指针
*							parmInfo: 要烧录的固件缓存指针
*							setProgress: 固件缓存长度
*							ptrClass: 烧录进度回调函数指针
*							fwBuf: 填0，则从设备Flash里面读取描述信息并进行更改后写入到Flash里面，否则从fwBuf指向的固件缓存中读取描述信息(fwBuf指向的固件缓存内容必须和设备固件内容一致)。
*							fwLen: fwBuf指向的固件数据字节数
*/
	bool WINAPI SonixCam_SetDevDescriptor(scDevice* dev, scDevDescriptor* parmInfo, SonixCam_SetProgress setProgress, void* ptrClass, uchar* fwBuf = 0, uint fwLen = 0);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	校验固件是否完整
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_FullCheckFW
*	Parameters:		
*							dev: 操作的设备参数指针
*							fwBuf: 要校验的固件缓存指针
*							fwLen: 固件缓存长度
*							setProgress: 校验进度回调函数指针
*							ptrClass:  传递给setProgress参数的指针
*/
	bool WinAPI SonixCam_FullCheckFW(scDevice* dev, uchar fwBuf[], uint fwLen, SonixCam_SetProgress setProgress, void* ptrClass);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//	导出固件
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ExportFW
*	Parameters:		
*							dev: 操作的设备参数指针
*							fwBuf: 要导出的固件缓存指针
*							fwLen: 固件缓存长度
*							setProgress: 导出进度回调函数指针
*							ptrClass:  传递给setProgress参数的指针
*/
	bool WinAPI SonixCam_ExportFW(scDevice* dev, uchar fwBuf[], uint fwLen, SonixCam_SetProgress setProgress, void* ptrClass);
	

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//打开摄像头设备
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_OpenCamera
*	Parameters:		
*							dev: 设备指针
*							hWnd: 浏览窗口句柄，可以填0
*							sampleGrab: 图像采集回调函数指针，可以填0
*							ptrClass: 传递给采集回调函数参数， 可以填0
*/
	bool WinAPI SonixCam_OpenCamera(const scDevice* dev, HWND hWnd, SonixCam_SampleGrabberBuffer sampleGrab, void* ptrClass = nullptr);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//关闭摄像头设备
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_CloseCamera
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_CloseCamera(const scDevice* dev);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//是否打开了摄像头
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_IsOpened
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_IsOpened(const scDevice* dev);



////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//摄像头是否正在浏览中
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_IsPreviewing
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_IsPreviewing(const scDevice* dev);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//调整浏览窗口（hWnd）大小和显示与隐藏
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_AdjustPreviewWindow
*	Parameters:		
*							dev: 设备指针
*							visable: 是否显示
*							x: x坐标
*							y: y坐标
*							right: 宽度
*							bottom: 高度
*/
	bool WinAPI SonixCam_AdjustPreviewWindow(const scDevice* dev, bool visable, uint x, uint y, uint right, uint bottom);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//开始浏览
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_StartPreview
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_StartPreview(const scDevice* dev);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//暂停浏览
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_PausePreview
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_PausePreview(const scDevice* dev);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//停止浏览
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_StopPreview
*	Parameters:		
*							dev: 设备指针
*/
	bool WinAPI SonixCam_StopPreview(const scDevice* dev);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得浏览或采样视频格式数量
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetFormatCount
*	Parameters:		
*							dev: 设备指针
*							formatCount: 返回格式数量
*/
	bool WinAPI SonixCam_GetFormatCount(const scDevice* dev, uint* formatCount);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得指定索引对应的视频格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetFormat
*	Parameters:		
*							dev: 设备指针
*							formatIndex: 要获得的格式索引号
*							outFormat: 返回指定索引号对应的视频格式
*/
	bool WinAPI SonixCam_GetFormat(const scDevice* dev, uchar formatIndex, scVideoOutFormat* outFormat);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//打开当前浏览或采样视频格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetPreviewFormat
*	Parameters:		
*							dev: 设备指针
*							format: 返回当前打开浏览的格式
*/
	bool WinAPI SonixCam_GetPreviewFormat(const scDevice* dev, scVideoOutFormat* format);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置浏览或采样视频格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetPreviewFormat	
*	Parameters:		
*							dev: 设备指针
*							formatIndex: 要设置的格式索引号
*/
	bool WinAPI SonixCam_SetPreviewFormat(const scDevice* dev, uint formatIndex, HWND preWndHandle = 0);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得Still Pin拍照格式数量
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetStillFormatCount
*	Parameters:		
*							dev: 设备指针
*							formatCount: 返回Still Pin的拍照格式数量
*/
	bool WinAPI SonixCam_GetStillFormatCount(const scDevice* dev, uint* formatCount);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得索引对应的Still Pin拍照格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetStillFormat
*	Parameters:		
*							dev: 设备指针
*							formatIndex: 格式索引号
*							outFormat: 返回当前索引对应的Still Pin拍照格式
*/
	bool WinAPI SonixCam_GetStillFormat(const scDevice* dev, uint formatIndex, scVideoOutFormat* outFormat);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得当前Still Pin设置的视频格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetCurStillFormat
*	Parameters:		
*							dev: 设备指针
*							format: 返回当前打开浏览的格式
*/
	bool WinAPI SonixCam_GetCurStillFormat(const scDevice* dev, scVideoOutFormat* format);



////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置Still Pin拍照格式
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetStillFormat
*	Parameters:		
*							dev: 设备指针
*							formatIndex: 设置当前Still Pin拍照格式
*/
	bool WinAPI SonixCam_SetStillFormat(const scDevice* dev, uint formatIndex);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Still Pin拍照
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_StillSnapshot
*	Parameters:		
*							dev: 设备指针
*							sampleGrab: Still Pin拍照图像缓存回调函数
*							ptrClass: 传递给sampleGrab的参数
*/
	bool WinAPI SonixCam_StillSnapshot(const scDevice* dev, SonixCam_SampleGrabberBuffer sampleGrab, void* ptrClass = nullptr);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得属性范围
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_PropertyGetRange	
*	Parameters:		
*							dev: 设备指针
*							property: 要获得的属性
*							min: 最小值
*							max: 最大值
*							step: 步长值
*							defValue: 默认值
*							flags: 默认属相标志
*/
	bool WinAPI SonixCam_PropertyGetRange(const scDevice* dev, scProperty property, long* min, long* max,
											long* step, long* defValue, scPropertyFlag* flags);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得属性项值
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_PropertyGet	
*	Parameters:		
*							dev: 设备指针
*							property: 要获得的属性
*							value: 返回获得的属性值
*							flags: 返回获得的属性标志
*/
	bool WinAPI SonixCam_PropertyGet(const scDevice* dev, scProperty property, long* value, scPropertyFlag* flags);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置属性
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_PropertySet
*	Parameters:		
*							dev: 设备指针
*							property: 要设置的属性
*							value: 要设置的属性值
*							flags: 要设置的属性标志值
*/
	bool WinAPI SonixCam_PropertySet(const scDevice* dev, scProperty property, long value, scPropertyFlag flags);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得控制范围
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ControlGetRange
*	Parameters:		
							dev: 设备指针
*							control: 要获得的控制属性
*							min: 最小值
*							max: 最大值
*							step: 步长值
*							defValue: 默认值
*							flags: 默认控制标志
*/
	bool WinAPI SonixCam_ControlGetRange(const scDevice* dev, scControl control, long* min, long* max,
											long* step, long* defValue, scPropertyFlag* flags);

////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得控制项值
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ControlGet	
*	Parameters:		
*							dev: 设备指针
*							control: 要获得的控制属性
*							value: 返回获得的值
*							flags: 返回获得的标志值
*/
	bool WinAPI SonixCam_ControlGet(const scDevice* dev, scControl control, long* value, scPropertyFlag* flags);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置控制项值
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_ControlSet	
*	Parameters:		
							dev: 设备指针
*							control: 要是之的控制属性
*							value: 要设置的控制值
*							flags: 要设置的控制标志值
*/
	bool WinAPI SonixCam_ControlSet(const scDevice* dev, scControl control, long value, scPropertyFlag flags);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得帧率
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetFrameRate	
*	Parameters:		
*							dev: 设备指针
*							fps: 获得当前浏览帧率
*/
	bool WinAPI SonixCam_GetFrameRate(const scDevice* dev, uchar* fps);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置帧率
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetFrameRate	
*	Parameters:		
*							dev: 设备指针
*							fps: 要设置的帧率
*/
	bool WinAPI SonixCam_SetFrameRate(const scDevice* dev, uchar fps);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得电力频域值
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetPowerLine	
*	Parameters:		
*							dev: 设备指针
*							powerLine: 获得当前的电力线频率
*/
	bool WinAPI SonixCam_GetPowerLine(const scDevice* dev, scPowerLine* powerLine);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置电力频率值
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetPowerLine	
*	Parameters:		
*							dev: 设备指针
*							powerLine: 设置电力线频率
*/
	bool WinAPI SonixCam_SetPowerLine(const scDevice* dev, scPowerLine powerLine);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//获得低亮度补偿状态
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_GetAutoExposurePriority
*	Parameters:		
*							dev: 设备指针
*							check: 返回当前自动曝光优先级值
*/
	bool WinAPI SonixCam_GetAutoExposurePriority(const scDevice* dev, long* check);


////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//设置低亮度补偿状态
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
*	Function:		SonixCam_SetAutoExposurePriority
*	Parameters:		
*							dev: 设备指针
*							check: 设置当前自动曝光优先级值
*/
	bool WinAPI SonixCam_SetAutoExposurePriority(const scDevice* dev, long check);

}

}


#endif // VIDEOCAPTURE_H

